// In javascript we can easily list all properties of an object which can be used to loop through if required easily
const car = {
    make: 'Toyota',
    model: 'Camry',
    year: 2023
}

const keys = Object.keys(car);

console.log(keys);
